import './bootstrap';
import Typed from 'typed.js';

const typed = new Typed(".multiple-text", {
    strings: ["IT OFFICER", "WEB DEVELOPER", "OFFICE ADMINISTRATION", "CONTENT WRITER"],
typeSpeed: 100,
backSpeed: 100,
backDelay: 1000,
loop: true
})
